#include <stdio.h>
struct node
{
    int data;
    struct node *left_child;
    struct node *right_child;
};
void insert(node **binary_tree, int value)
{
    node *temp = NULL;
    if (!(*binary_tree))
    {
        temp = (node *)
    }
}